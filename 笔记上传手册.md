# 笔记上传与发布手册

本文说明如何把本地 Markdown 笔记（含封面/配图）写入本仓库，并一键推送到 GitHub、更新 VPS 上的博客。

更细的 Frontmatter / Markdown 语法见 [docs/WRITING.md](docs/WRITING.md)；Docker / VPS 部署见 [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)。

---

## 1. 目录约定

| 路径 | 用途 | 是否进 Git |
|------|------|------------|
| `content/<分类ID>/*.md` | 文章真相源 | ✅ |
| `public/images/` | 文内配图 / 封面（推荐） | ✅ |
| `public/uploads/` | 后台附件上传区 | ❌（gitignore，需另同步） |

预设分类 ID：`ai` · `computer-vision` · `huawei-datacom` · `web-dev` · `project-management` · `life` · `others`

示例：

```text
content/computer-vision/traditional-cv-complete-guide.md
public/images/traditional-cv-cover.png
```

---

## 2. 准备一篇笔记

### 2.1 Frontmatter 模板

文件开头必须有 YAML：

```yaml
---
title: 文章标题
slug: my-article-slug          # 英文短横线；决定 URL /post/<slug>
category: computer-vision      # 必填，见上方分类 ID
tags:
  - opencv
  - image-processing
status: published              # draft | published
publishedAt: 2026-07-14
summary: >
  一两百字摘要，用于列表与 SEO。
# series: OpenCV 入门教程     # 可选：专题名
# subcategory: 第3章 图像滤波  # 可选：章节名
# order: 12                    # 可选：专题内顺序
---

正文从这里开始…
```

### 2.2 图片路径（重要）

不要使用本机绝对路径（如 `D:\笔记\xxx.png`），否则线上无法显示。

推荐：

1. 把图片放到 `public/images/`（文件名尽量英文）
2. Markdown 中写站点路径：

```markdown
![封面](/images/traditional-cv-cover.png)
```

后台「附件管理」上传的图路径形如 `/uploads/...`，也可使用。

### 2.3 本地预览（Docker）

```powershell
docker compose --profile rag up -d --build
# 浏览器打开 http://localhost:3000
# 后台登录 → 设置 →「从文件系统同步」
```

`content/`、`public/images/`、`public/uploads/`、`prisma/` 已通过 compose 挂载；改 MD 后同步即可，不必每次重建镜像（**改代码**才需要 `--build`）。

---

## 3. 一键发布（推荐）

### 3.1 首次配置部署密钥

```powershell
copy .deploy.env.example .deploy.env
# 用编辑器填写 VPS_HOST / VPS_USER / VPS_PASSWORD / VPS_REPO_DIR 等
```

`.deploy.env` 已在 `.gitignore` 中，**切勿提交到 Git**。

### 3.2 日常发布流程

1. 写好 `content/.../*.md` 与配图  
2. 双击根目录 **`publish.bat`**，或在项目根执行：

```powershell
.\publish.bat
# 或
.\scripts\publish.ps1 -Message "新增某某笔记"
```

脚本会依次：

1. `git add` 笔记与图片相关路径  
2. `git commit`（若有变更）  
3. `git push origin main`  
4. SSH 到 VPS：`git pull` → `docker compose up -d --build` → 调用 `/api/sync`

也可以只更新服务器（代码已在 GitHub 上）：

```powershell
.\scripts\deploy-vps.ps1
```

### 3.3 管理后台一键发布

在 VPS / 本机已配置 `.deploy.env`（或 `DEPLOY_HOOK_URL`）后，管理员可登录后台 → **设置**：

| 按钮 | 作用 |
|------|------|
| **推送到 GitHub** | `git add content public/images` → commit → `push` |
| **部署到 VPS** | 调用 `scripts/deploy-vps.py`，或 `POST DEPLOY_HOOK_URL` |

说明：

- 界面**不展示、不落库**密码；只显示「就绪 / 未配置」。
- 应用若跑在 **Docker 容器内**且无法 SSH 宿主机，请配置 `DEPLOY_HOOK_URL` 指向宿主机上的部署 hook（见 `.deploy.env.example`）。
- EDITOR 角色看不到设置页；仅 ADMIN 可触发。

### 3.4 手动命令（不跑批处理时）

```powershell
git add content public/images
git commit -m "docs: 新增某篇笔记"
git push origin main
.\scripts\deploy-vps.ps1
```

VPS 默认定目录为 `/var/www/blog/Zoo-Blog`（可在 `.deploy.env` 修改）。

---

## 4. 校验清单

发布后打开：

- 首页是否出现新笔记  
- `/post/<slug>` 正文与封面  
- 宽屏下右侧目录、顶部阅读进度条  
- `/search?q=关键词`（Docker 下应为 Meilisearch）

若文章未入库：VPS 后台 **设置 → 从文件系统同步**，或确认 `.env` 中 `SYNC_SECRET` 与脚本一致。

---

## 5. 常见问题

| 现象 | 处理 |
|------|------|
| 封面裂图 | 检查是否仍为 `D:\...` 绝对路径；应改为 `/images/...` |
| 同步未授权 | `SYNC_SECRET` 与服务器 `.env` 不一致 |
| 推送失败 | 配置 GitHub SSH 密钥；`git remote -v` 确认指向本仓库 |
| VPS 仍是旧版 | `deploy-vps.ps1` 是否成功；服务器上 `git log -1` |
| 搜索仍像 SQLite | 确认 meilisearch 容器运行，且 compose 覆盖了 `MEILISEARCH_HOST` |

---

## 6. 安全提示

- 不要把 VPS 密码写进仓库或发给公开聊天  
- 优先改用 SSH 公钥登录，并轮换已暴露过的密码  
- `.env` / `.deploy.env` / `.sync.env` 均不可提交  
